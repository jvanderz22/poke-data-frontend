<nav class="navbar navbar-inverse navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      {{link-to 'Poke-Data' 'application' class="navbar-brand"}}
    </div>

    <ul class="nav navbar-nav">
      {{#link-to 'teams.create' tagName='li'}}
        {{link-to 'Create Team' 'teams.create'}}
      {{/link-to}}

      {{#if session.isAuthenticated}}
        {{#link-to 'logout' tagName='li'}}
          {{link-to 'Logout' 'logout'}}
        {{/link-to}}
      {{else}}
        {{#link-to 'login' tagName='li'}}
          {{link-to 'Login' 'login'}}
        {{/link-to}}
      {{/if}}
    </ul>
  </div>
</nav>

<div class="container">
  {{outlet}}
</div>
